import 'package:flutter/material.dart';

class IdCardCheck extends StatelessWidget {
  const IdCardCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
